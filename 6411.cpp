#include <cstdio>
#include <algorithm>
#include <vector>
#include <cmath>
const int N = 1e5 + 5;
int n, m, s, t;

int main(){
    scanf("%d%d%d%d", &n, &m, &s, &t);
    for (int i = 1; i <= m; i++){

    }
    return 0;
}