#include <cstdio>
#include <algorithm>
#include <vector>
#include <cmath>
#include <iostream>
#include <string>
#include <cstring>
const int N = 2e5 + 5;
int n, k, dep[N];
std::vector<int> st[N];
void dfs(int u, int fath){
    dep[u] = dep[fath] + 1;
    for (auto v : st[u]){
        
    }
}
int main(){
    scanf("%d%d", &n, &k);
    for (int i = 1; i < n; i++){
        
    }
    return 0;
}