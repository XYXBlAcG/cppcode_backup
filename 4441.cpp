#include <cstdio>
const int N = 1e5 + 5;
int n, m, a[N];
int main(){
    scanf("%d%d", &n, &m);
    for (int i = 1; i <= n; i++) scanf("%d", a + i);
    
    for (int i = 1; i <= m; i++){
        int op, x, l, r, c;
        scanf("%d", &op);
        if(!op){
            
        }else{
            
        }
    }
    return 0;
}