#include <cstdio>
#include <vector>
const int N = 1e5 + 5;

int T, n, m;
void dfs(int u){

}
std::vector<int> st[N << 2];
int main(){
    scanf("%d%d%d", &T, &n, &m);
    for (int i = 1, c, t, x, y; i <= m; i++){
        scanf("%d%d%d%d", &c, &t, &x, &y);
    }
    
    return 0;
}