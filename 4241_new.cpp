#include <cstdio>
#include <algorithm>
#include <cmath>
const int N = 1e5 + 5, mod = 1e9 + 7;
int T, n, rec[N], vis[N];
int cal(int o){
    if(vis[o]) return rec[o]; vis[o] = 1;
    for (int j = 1; j <= o; j++){
        
    }
}
void solve(){
    for (int i = 1; i <= n; i++){
        
    }
}
int main(){
    scanf("%d", &T);
    while(T--){
        scanf("%d", &n);
        solve();
    }
    return 0;
}