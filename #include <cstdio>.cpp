#include <cstdio>
int n, m;
int main(){
    scanf("%d%d", &n, &m);
    for (int i = 1; i <= m; i++){
        int op, a, b;
        scanf("%d", &op);
        if(op < 3){
            scanf("%d%d", &a, &b);
        }else{
            
        }
    }
    return 0;
}