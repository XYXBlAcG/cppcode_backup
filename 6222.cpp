#include <cstdio>
#include <algorithm>
#include <vector>
#include <cmath>
#include <string>
#include <iostream>
using std::cin;
using std::cout;
using std::string;
const int N = 1e5 + 5;
int n, m;
int main(){
    std::ios::sync_with_stdio(0);
    cin >> n >> m;
    for (int i)
    return 0;
}