#include <cstdio>
#include <algorithm>
#include <vector>
#include <cmath>
#include <iostream>
#include <string>
#include <cstring>
const int N = 1e5 + 5;
int T, n, m, v[N];
int main(){
    scanf("%d", &T);
    while(T--){
        scanf("%d%d", &n, &m);
        // for (int i = 1; i <= n; i++) 
    }
    return 0;
}