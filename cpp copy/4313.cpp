#include <cstdio>
#define int long long
const int N = 9, mod = 1000000007;
int n, m, a[N];
signed main(){
    scanf("%lld%lld", &n, &m);
    for (int i = 1; i <= m; i++){
        scanf("%lld", a + i);
    }
    return 0;
};