#include <cstdio>
#include <iostream>
int n, f, a;
char name[20][20];
int id[20];
int have[20];
int folder[20];
void dfs(int in){
    if ()
}
int main(){
    scanf("%d",&n);
    for (int x = 1; x <= n; x++){
        scanf("%d%d",&f,&a);
        have[x] = f;
        std::cin >> name[x];
        for (int i = 1; i <= a; i++){
            std::cin >> id[x];
        }
    }
    dfs(1);
    return 0;
}
