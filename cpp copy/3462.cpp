#include <cstdio>
typedef long long ll;
const int N = 15;
int n, a[N], b[N]; bool vis[N][1 << N]; ll rec[N][1 << N];
ll f(){
    
}
int main(){
    scanf("%d",&n);
    for (int i = 0; i < n; i++) scanf("%d",&a[i]);
    for (int i = 0; i < n; i++) scanf("%d",&b[i]);
    return 0;
}