#include <cstdio>
#define int long long
const int N = 1e3 + 5;
int c1, c2, c3, c4, tot, d1, d2, d3, d4, s;
signed main(){
    scanf("%lld%lld%lld%lld%lld", &c1, &c2, &c3, &c4, &tot);
    for (int i = 1; i <= tot; i++){
        scanf("%lld%lld%lld%lld%lld", &d1, &d2, &d3, &d4, &s);
        
    }
    return 0;
}