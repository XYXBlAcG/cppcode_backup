H2.2.4.1  太难不做

定义：f(i,j)为在第i天，有第j支股票的总价格
答案：f(1,1);
方程：
max(
    f(i + 1, j);
    max(k = 0;k ∈ [1..m]){
        f(i + 1, k);
        f(i, k);
    }
)
边界：
复杂度：

H2.2.4.2

定义：f(i,j)表示从前i个数里面选出一些，以a[i]作为第j段中的某个数，能


(()(()))()