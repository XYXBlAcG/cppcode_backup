#include <cstdio>
const int N = 1e5 + 5;
template<typename T>
struct Node{
    T data[N]; int l, r;
    void push_back(T a){
        data[++r] = a;
    }
    void push_front(T a){
        data[]
    }
};
int main(){
    return 0;
}