#include <cstdio>
#define int long long
const int N = 1e6 + 5;
int n, xx[N], yy[N];
signed main(){
    scanf("%lld", &n);
    for (int i = 1; i <= n; i++){
        scanf("%lld%lld", xx + i, yy + i);
    }
    
    return 0;
}