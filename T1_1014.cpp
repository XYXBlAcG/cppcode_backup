#include <cstdio>
#include <algorithm>
#include <vector>
#include <cmath>
#define int long long
const int N = 1e5 + 5;
int n, a[N], ans, f[N];
signed main(){
    scanf("%lld", &n);
    for (int i = 1; i <= n; i++) scanf("%lld", a + i);
    
    return 0;
}