#include <cstdio>
#include <string>
int n;
int main(){
    scanf("%d ", &n);
    for (int i = 1; i <= n; i++){
        std::string element;
        for (char c = getchar(); c != '\n'; c = getchar()){
            if(c >= '0' && c <= '9'){
                
            }
        }
    }
    return 0;
}