#include <cstdio>
#include <algorithm>
#include <vector>
#include <cmath>
const int N = 10005, M = 105;
int n, m, v[M], w[M], rec[N][M];
int dp(int i, int j){
    
}
int main(){
    scanf("%d%d", &n, &m);
    for (int i = 1; i <= n; i++){
        scanf("%d%d", w + i, v + i);
    }
    
    return 0;
}