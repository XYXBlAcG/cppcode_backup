#include <cstdio>
const int N = 1e7 + 5, M = 105, NM = 1e5 + 5;
int n, m, lm, cnt[NM];
char mo[N], text[NM][M];
struct Trie{
    
};
int main(){
    scanf("%d%d", &n, &m);
    scanf("%s", mo);
    for (int i = 1; i <= m; i++)
        scanf("%s", text[i]);
    
    return 0;
}