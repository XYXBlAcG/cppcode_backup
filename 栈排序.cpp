#include <cstdio>
#include <algorithm>
#include <vector>
#include <cmath>
const int N = 1e5 + 5;
int T, n, a[N];
void clean(){
    memset(a, 0, sizeof(a));
    for (int i = 1; i <= n; i++){
        
    }
}
void solve(){

}
int main(){
    scanf("%d", &T);
    while(T--){
        scanf("%d", &n);
        for (int i = 1; i <= n; i++) scanf("%d", a + i);
        solve();
    }
    return 0;
}